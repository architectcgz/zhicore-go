# Tests

Test placement:

- `tests/architecture`: source-level architecture checks.
- `tests/system/http`: black-box HTTP scenarios.
- `tests/runtime`: integration tests requiring real processes or external services.
- `tests/testkit`: reusable black-box fixtures and assertions.
