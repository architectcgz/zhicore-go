# zhicore-ops

`zhicore-ops` 是迁移运维工具服务的 Go 迁移模块。

服务职责：

- 拥有回滚记录、对账记录、CDC 修复和迁移期运维状态。
- 为 Java 到 Go 迁移提供对账、回滚和 CDC 修复能力。

数据归属：

- Ops Redis key
- 对账、回滚、CDC checkpoint 等迁移状态

迁移注意点：

- Ops 可以检查或修复业务数据，但不因此拥有业务数据。
- 长期产品功能不应依赖 Ops 的迁移内部实现。
- 当前开发阶段不做灰度，不引入灰度配置或用户灰度标记。
