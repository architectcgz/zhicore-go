# Services

Each directory under `services/` is an independently buildable, testable, and deployable Go service.

Every service owns:

- `go.mod`
- `cmd/server/`
- `internal/`
- `api/`
- `configs/`
- `migrations/`
