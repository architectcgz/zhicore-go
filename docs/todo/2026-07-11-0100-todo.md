# 移出事务内跨服务调用

- Project: `/home/azhi/workspace/projects/zhicore-go/.worktrees/notification-api`
- Created: `2026-07-11T01:00+08:00`
- Status: `Open`

## Context

通知契约审查发现：跨服务同步 HTTP 不应在本服务数据库事务持锁期间执行；已确认采用事务前读取/校验快照或 guard 的方式，不维护本地 profile projection。

## Open Items

- [ ] 将 Content 点赞的 User actor snapshot 查询移到 Content 写事务前，并保持 outbox 使用该已校验快照。
- [ ] 将 Comment 创建的 User author snapshot 查询移到 Comment 写事务前，并把快照传入事务内 outbox 写入。
- [ ] 将 Comment 点赞的 User 拉黑关系校验移到 Comment 写事务前；保留事务内 comment 状态与 like 幂等 authoritative 校验。
- [ ] 修复 Notification group state 对乱序事件无条件覆盖 latest 字段的问题；只在事件排序键更新时替换展示状态。
- [ ] 将 Notification 保存的 content title 和 target snapshot 纳入聚合 / rebuild 查询，并直接映射到列表响应。
- [ ] 在 Vue 组级已读成功后处理并发 `changedCount` / `unreadCount`，刷新 unread facts；为 actor 详情引入会话 token 防止同组重开时的 stale response 覆盖。
- [ ] 修复 group state 部分缺失时的 inbox fallback 合并，并补齐 groupId 格式校验与真实 PostgreSQL 读取 / 已读 integration coverage。
