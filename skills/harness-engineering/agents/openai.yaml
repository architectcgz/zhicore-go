interface:
  display_name: "Harness Engineering"
  short_description: "Initialize repository AI-agent harnesses"
  default_prompt: "Use $harness-engineering to initialize a repository harness with AGENTS navigation, feedback loops, and mechanical checks."
policy:
  allow_implicit_invocation: true
