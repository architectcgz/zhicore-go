interface:
  display_name: "Harness Router"
  short_description: "Route tasks into repository harness workflows"
  default_prompt: "Use $harness-router to decide whether this task should enter the repository harness workflow before implementation."
